function Header(){
  return (
    <header className="header">
      <h1>💼 Job Board</h1>

      <nav>
        <a href="#">Find Jobs</a>
        <a href="#">Post Job</a>
        <a href="#">Categories</a>
      </nav>
    </header>
  )
}

export default Header